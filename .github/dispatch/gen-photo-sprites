Dispatch marker for .github/workflows/gen-photo-sprites.yml — touch and push with a
`ci(dispatch):` head-commit message to trigger a run without workflow_dispatch access.
run: plate + berlines + table_apres — gate v5 PASS, kontext via POLLINATIONS_TOKEN
run 2: v1 assets supprimés pour régénération (plate + poses), gate v5
run 3: compte rechargé (7,01 pollen)
run 4: plate seule — kontext img2img via gen.pollinations.ai (paid farm, résolution exacte
  2048x1152 + conditionnement image= honoré, cf. gen-photo-sprites.mjs)
run 5: fix rawUrl (public/ manquant, 404 sur le crop) — replate
run 6: A/B modele plate — nanobanana-2 (kontext ignore width/height, sort JPEG 1024x1024)
run 7: fix resize-to-target (kontext JPEG 1024x1024 -> stretch PNG 2048x1152), retour kontext
run 8: plate seule — deux temps: flux angle plongeant (texte seul, sans conditionnement)
  PUIS kontext img2img sur ce rendu (data uri, pas de crop frontal en reference) — le
  conditionnement sur l'elevation frontale ecrasait le texte dormer, cf. gen-photo-sprites.mjs
run 8b: touch to satisfy paths filter (empty commit above changed no path)
run 8 RESULT: stage 2 (kontext, data-uri ref) 414'd — url too long for a 2048x1152 png
  in a query string; shipped the pre-authorised stage-1 (flux, angle-only) fallback.
  Fixed (this commit): stage 2 now uploads via multipart POST /v1/images/edits
  (editImagePaid) instead of a GET data-uri. BUT stage 1 alone (flux, dormer-POV prompt,
  NO conditioning at all) still shipped an EYE-LEVEL street, not the plunging rooftop
  view the prompt asks for — visible in the committed plate.png at b9f49e5c. That is a
  prompt-strength issue (flux not honouring the dormer/plunging framing clause), not a
  conditioning fight, so re-dispatching stage 2 alone won't fix it — needs concept-artist
  to re-weight/front-load the framing tokens (or an explicit "bird's-eye view down onto"
  phrasing) before the next run.
run 9: plate as a single kontext edit of plate-v2-reference.png (bertrand's validated
  props/register, plain-pied route decided in spec-photo-qte-fiction.md Rev.5). Result
  (a4f4f82e) preserved everything validated (night, trait, trame, facades — the real risk)
  but applied none of the three requested changes (no terrace, pedestrians still centred,
  signage still lettered) — kontext read "preserve", not "transform". No strength/guidance/
  fidelity param exists on POST /v1/images/edits (checked APIDOCS.md, not assumed).
run 10 (next, blocked): plateAsset.editChain — ONE VARIABLE PER EDIT. Stage 1
  `plate_edit_a` (photoQte.plateEditA, TODO concept-artist): clear the pavement of the
  eight pedestrians, blank the two signs. Stage 2 `plate` (photoQte.plateEditB, TODO
  concept-artist): add the terrace on Stage 1's OWN committed output. Do not dispatch
  either stage until its prompt key is written — loadPhotoQte() now throws per-stage if
  it's missing. Also fixed since run 9: the plate is padded to a square before upload and
  cropped back after (kontext's fixed 1024x1024 edit response was being stretched
  anisotropically onto 16:9, warping every straight line — see gen-photo-sprites.mjs).
run 11: plate_edit_a alone (2-stage chain). RESULT (bc379886): people gone, signage
  blanked — BUT every glazed surface (shop windows, upper-floor windows) also went to a
  flat white blank, and the validated scooter+bicycle vanished. "Clear the people out of
  the street" over-generalised from "the people" to "the street" — one instruction, one
  class of surfaces wiped, no force knob to contain it. plate-edit-a.png from this run is
  KNOWN-REGRESSED, dropped from disk, never a reference for anything downstream.
run 12: plate_edit_a alone, 3-stage chain, keys/prompts unchanged from c95393e5. RESULT
  (4083d6bc): the identical instruction that over-applied last time did NOTHING this time
  — pedestrians still centred, both signs still lettered. Two rolls, two opposite failure
  modes on the SAME instruction ⇒ not controllable at this granularity, stop rolling it.
BERTRAND DECISION (2026-08-05): pedestrians are KEPT (they stand mid-road, not on the
  terrace's pavement — the gate rule doesn't apply to them, and a lived-in street reads
  better). Signage is blanked by a SCRIPTED retouch, not a generative edit —
  scripts/retouch-photoqte-signage.mjs, deterministic, run once on plate-v2-reference.png
  → committed assets/photoqte/plate-signage-blanked.png. plateEditA/plateEditA2 are
  retired (kept in levelArt.json for the record, no longer wired). editChain is back to
  ONE stage: plate edits plate-signage-blanked.png with plateEditB (the terrace) — the
  only remaining transformation that needs a model.
run 13: dispatch `plate` alone (single remaining stage, editChain: plate edits
  plate-signage-blanked.png with plateEditB — the terrace). Stale plate.png/plate_mobile.png
  from run 9 (a4f4f82e, an unrelated failed edit of the old v2 reference) removed from disk
  so this push-triggered run (no workflow_dispatch force input available) regenerates it
  instead of skipping. plateAsset.editChain confirmed single-stage, pad-to-square/crop-back
  and assertDimensions already wired (gen-photo-sprites.mjs).
run 15: plate en texte-vers-image sur ideogram-v4-quality (modèle de street-wide.png), registre trait encré
run 16
run 17: plate ideogram t2i, textToImage=true
run 18: force t2i ideogram
run 19: plate supprimée, t2i ideogram
run 20: t2i ideogram + normalisation png
run 20: t2i ideogram + normalisation png
