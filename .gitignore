.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/sdks
!.yarn/versions

# Whether you use PnP or not, the node_modules folder is often used to store
# build artifacts that should be gitignored
node_modules

# Swap the comments on the following lines if you wish to use zero-installs
# In that case, don't forget to run `yarn config set enableGlobalCache false`!
# Documentation here: https://yarnpkg.com/features/caching#zero-installs

#!.yarn/cache
.pnp.*

# Environment
.env
.env.local

# Production build
dist/
dist-catalog/

# Test coverage
coverage/

# OS noise
.DS_Store
*.local

# Debug overlays from scripts/gen-window-zones.mjs --debug
scripts/.dbg-*.jpg
# Per-window crop evidence from the align-windows harness verification
scripts/.dbg-crops/
# scripts/slice-portrait-plate.mjs --explore-faces output — reconnaissance
# only, uploaded as a CI artifact, never committed
scripts/.dbg-portrait-explore/

# CodeGraph local index (rebuilt per machine via `codegraph index`)
.codegraph/

# E2E smoke shots — regenerated each run, uploaded as CI artifacts
screenshots/e2e-home.png
screenshots/e2e-ingame.png
screenshots/e2e-ingame-*.png

# Local verification / Gate-4 evidence captures — never committed
screenshots/verify/

# Vehicle-halo preview shots — regenerated each run by scripts/preview-vehicle-halo.mjs
screenshots/preview-vehicle-*.png
screenshots/e2e-delivery.png

# ADR-0005 dynamic-verification harness — regenerated each run, uploaded as CI
# artifacts. screenshots/golden/ is NOT ignored — those baselines are committed.
screenshots/motion-belliard.png
screenshots/golden-candidate-*.png

# session-local screenshots / throwaway capture scripts (never commit)
scratchpad/

# bake-off scratch output (regenerated by scripts/bakeoff-boss-models.mjs)
bakeoff-out/
style-test-out/

# Isolated worktrees created by background agents (transient)
.claude/worktrees/

# Sondes de recherche de prompt (transitoires, jamais du code de production).
# `.plate-score.mjs` est le scoreur COMMUN d'une vague d'exploration : il génère
# une planche depuis un prompt + graine et la note contre les gates de production
# (fond, silhouette, mèches débordantes, front blanc, coutures). Les `.probe-NN.mjs`
# sont les sondes d'une lane, une par hypothèse. Ils vivent le temps d'une vague et
# sont supprimés après — s'ils s'avèrent durablement utiles, ils remontent dans
# `scripts/` avec des tests, ils ne restent pas à la racine.
.plate-score.mjs
.probe-*.mjs
