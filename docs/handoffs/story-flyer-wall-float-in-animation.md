# Story — animation d'entrée du mur de flyers (NIVEAUX)

**PR :** [#145](https://github.com/bczy/prohimuf/pull/145) · **branche :** `claude/flyer-wall-float-in-animation`
**Palier : 🚚 TOURNÉE COMPLÈTE** — escaladé depuis la course express, voir §Palier.

## Quoi

Les flyers de NIVEAUX apparaissaient tous d'un coup. Ajout d'une entrée en cascade
(`mufFlyerFloatIn`, 1400 ms, staggée 180 ms par flyer via `--slot-delay`), avec trois
trajectoires de chute déterministes cyclées par index pour qu'aucune feuille ne rejoue
le chemin de sa voisine. Itéré en direct avec Bertrand (3 tours : « trop rapide », « trop
linéaire », « toujours la même distance gauche-droite »).

## Palier — pourquoi cette story existe

Ouverte en **course express** (un lane, `dev-r3f-render`, polish sur un composant déjà
gaté). Le palier a cassé en cours de route : un finding du panel a montré que
`screenshot-preview.mjs` capturait le menu en pleine animation, et le correctif correct
vivait dans `scripts/` — donc chez `dev-tooling-assets`.

Première tentative de résolution : une **dérogation de palier** demandée à
`senior-architect`, qui l'a accordée en argumentant que le critère mono-lane est une
heuristique de chemin approximant le rayon de souffle, et qu'ici l'heuristique se
déclenche sans que le rayon de souffle suive.

**Le panel l'a bloquée, à raison.** COLLABORATION.md §fix lane est absolu : un correctif
qui casse un critère en vol « ESCALATES to the full pipeline at the stage it violated —
**it never continues in the fix lane** ». Aucune clause de dérogation n'existe ;
l'architecte avait d'ailleurs reconnu inventer une troisième issue. Une dérogation
attribuable reste une dérogation à une règle qui n'en prévoit pas.

**Escaladé pour de bon** : palier tournée complète, cycle tracé ici et non dans
`docs/handoffs/fixes.md` (réservé aux cycles fix-lane), signature du second lane obtenue.

**Écart de doctrine à porter par `producer`** : le palier fix-lane n'offre que deux
issues (express / pipeline complet). Le cas rencontré — un correctif de review qui fait
déborder le diff sur un second lane — n'a pas d'issue proportionnée. Une troisième,
« express multi-lane à double signature », est à débattre pour COLLABORATION.md §fix lane.
Elle n'a PAS été appliquée ici : on ne s'autorise pas une exception avant qu'elle soit
doctrine.

## Signatures de lane

- **`dev-r3f-render`** — moitié render (`src/render/ui/**`).
- **`dev-tooling-assets` (Amelia) : SIGN-OFF** sur la moitié `scripts/` —
  `waitForFlyerWallSettled` interroge le bon élément (`.muf-flyer-slot` est bien le nœud
  animé), est sûr sous reduced-motion par construction (`animation: none` ⇒
  `getAnimations()` vide ⇒ prédicat vrai au tick suivant), la garde tableau-vide est
  porteuse (FlyerWall se démonte à chaque aller-retour de rubrique), le timeout de 20 s
  vaut ~8× le pire cas mesuré, et l'export additif est sans risque pour les 12
  importateurs dont les gates bloquants ; la suppression de la capture périmée puis
  re-throw sur le `00_menu.png` autoritaire est le bon arbitrage vu que `preview.yml` ne
  gate rien et que chaque capture y est déjà best-effort.
- **`senior-architect`** — analyse du rayon de souffle conservée comme argumentaire (elle
  reste juste sur le fond), mais elle ne vaut PAS dérogation : c'est la double signature
  de lane qui autorise le merge, pas elle.

## Vérification

`tsc` / `eslint` / `prettier` verts · `src/render/ui` 114 tests verts · panel CI 4
reviewers passé COMPLET et PASS (ADR-0063). Tests validés **par mutation** : pas de
stagger → 2 ms ⇒ rouge ; typo dans le littéral `"levels"` ⇒ rouge ; clé `--fio-*`
inconnue ⇒ `TS2353` ; attente de settle cassée ⇒ le script poursuit et supprime la
capture périmée au lieu de la publier.

## Assumé, hors périmètre

Ticket `ux-designer` à ouvrir : l'auto-focus du premier lancement part pendant l'entrée,
et l'entrée rejoue à chaque retour sur NIVEAUX. Aucun des deux n'est créé ni aggravé par
ce diff.

## Journal détaillé des 11 tours de panel

- 2026-07-26 · claude/flyer-wall-float-in-animation · dev-r3f-render · les flyers de l'écran NIVEAUX apparaissaient tous d'un coup ; ajout d'une animation d'entrée en cascade (`mufFlyerFloatIn`, 1400ms, dérive en zigzag) staggée 180ms par flyer via `--slot-delay`, deux garde-fous reduced-motion existants repris à l'identique — Bertrand feedback itératif sur un artifact de démo (v1 jugée « real quick », v2 ralentie + dérive ajoutée, validée) · simplify: commentaires référençant l'historique de la conversation dégraissés (2 lignes, comportement inchangé) · bug trouvé en review et corrigé : `scripts/screenshot-preview.mjs` capturait 00_menu.png avant que l'entrée soit stabilisée (`data-flyers-armed` ne gate que le click-through, pas le settle visuel) → d'abord corrigé par un `sleep(2500)` (commit `c0d0593a`), mécanisme REMPLACÉ plus tard dans la MÊME PR après le panel stage 6 : la PR livre en réalité `waitForFlyerWallSettled()` (`scripts/e2e-lib.mjs`), une attente de l'état final observable via la Web Animations API, câblée sur les deux captures du menu — voir l'entrée du 2026-07-30 ci-dessous. AUCUN `sleep` de durée devinée n'est livré : ce serait précisément la classe de fragilité (dérive stagger/durée/`LEVELS.length`) que le correctif final élimine · checks: tsc/vitest(31 render/menu)/lint verts · review: code-review(high) — 1 CONFIRMED (screenshot mid-animation, fixé) + 2 PLAUSIBLE (focus auto-scroll pendant l'animation en premier lancement/rack short-landscape ; pas de test dédié sur le stagger/reduced-motion) laissés en l'état, mineurs et hors scope du fix rapide
- 2026-07-26 · claude/flyer-wall-float-in-animation · dev-r3f-render · suite directe (3 tours de feedback en jeu de Bertrand sur la même entrée `mufFlyerFloatIn`) : (a) « un peu moins linéaire » → chaque stop de transform porte sa propre `animation-timing-function` (chute accélérée `cubic-bezier(0.55,0,1,0.45)`, balancements décélérés `ease-out`/`ease-in-out`, settle en léger back-out `cubic-bezier(0.34,1.56,0.64,1)`) au lieu d'un `ease-in-out` plat sur toute la durée ; (b) « c'est tout le temps la même distance gauche droite, ça fait pas naturel » → valeurs de transform paramétrées en custom properties (`--fio-{y,x,r}0..3`) et `FLYER_FLOAT_IN_VARIANTS` (3 jeux déterministes, cyclés en `i % 3` comme `FLYER_REST_ROTATION_DEG`/`FLYER_JITTER_PX`, zéro `Math.random`) posés en style inline par slot ⇒ hauteur de chute, sens+amplitude de dérive et rotation distincts d'un flyer à l'autre ; (c) « fais tomber les feuilles d'un peu plus haut, synchronise la vitesse de chute avec les mouvements actuels » → Y des 3 variantes ×~1.35 (A : −140 → −190px, B −230px, C −142px) à stops (0/22/38/60/80/100 %) et courbes/X/rotations INCHANGÉS, donc la chute plus longue retombe sur les mêmes temps de zigzag · pourquoi des valeurs et pas des `@keyframes` nommés par flyer : vérifié sur le build réel de ce projet (Vite sans bloc `css.modules`, donc scoping CSS Modules par défaut) — `@keyframes mufFlyerFloatIn` sort hashé en `_mufFlyerFloatIn_j2ser_1` dans `dist/assets/*.css`, un nom littéral écrit depuis le JS ne matcherait jamais ; les NOMS de custom properties, eux, traversent le build à l'identique (12 × `--fio-*` intacts) · corrigé sur le brouillon : `animation-timing-function: ease-out` du stop 22 % supprimée (stop qui ne déclare que `opacity`, laquelle vaut déjà 1 jusqu'à 100 % ⇒ courbe morte ; l'intervalle de transform qui l'englobe est 0 %→38 % et est piloté de bout en bout par la courbe du stop 0 %, subtilité désormais documentée dans le bloc) + commentaire TSX qui présentait `FLYER_REST_ROTATION_DEG`/`FLYER_JITTER_PX` comme définis « above » alors qu'ils sont importés de `@render/ui/print` · checks: tsc/eslint/prettier/vitest(31 render/menu) verts + preuve navigateur (`vite preview` + Playwright) : les 5 slots reçoivent bien A/B/C/A/B (`--fio-y0` = −190/−230/−142/−190/−230px, délais 0→720ms), état stabilisé `transform: matrix(1,0,0,1,0,0)` / `opacity: 1` identique à la capture déjà en PR sur 390×844, 1280×800 et le rack 844×390, garde-fou reduced-motion toujours inerte (`animation-name: none`) · signalé hors périmètre (pré-existant, non corrigé) : la dérive horizontale crée un dépassement de scroll transitoire dans `.rubriques` de MainMenu pendant l'entrée — 34px avant ce tour, 50px après sur 390×844 (31px sur 1280×800, 0 sur le rack) ; retombe à 0 en ~800ms, aucun dépassement sur `documentElement`, mais sur un OS à barres de défilement classiques ça peut faire clignoter une barre horizontale. Correctif propre (`overflow-x: clip` + `overflow-clip-margin` sur un conteneur partagé avec le rack et la grille desktop) = changement de comportement à gater par Bertrand, hors tuning d'animation · 2026-07-30 (tour 4, verdict DEGRADED — `edge-case-hunter` n'a pas tourné, donc verdict NON autoritaire) : le BLOQUANT visait le corps de la PR lui-même, qui affirmait encore « Panel CI stage 6 : PASS (0/0/2) » — vrai au tour 1, FAUX dès le tour 2 (FAIL 1/2/4). Une case cochée qui ment au merge gate est pire qu'une case vide : décochée, remplacée par le tableau honnête des 4 tours et de leurs verdicts, avec rappel que l'autorité est le check `panel-verdict`, pas une case du corps de PR. La case mono-lane, elle, était déjà décochée depuis `42e8614d` · 2026-07-30 (tour 5, DEGRADED — `edge-case-hunter` ET `bmad-review` n'ont pas tourné, verdict NON autoritaire ; 0 bloquant / 0 majeur / 1 mineur) : le commentaire de `.rubriques` se contredisait — en passant de `clip` à `hidden` j'avais AJOUTÉ le paragraphe explicatif sans SUPPRIMER celui qui plaidait pour `clip`, et qui citait en plus une `overflow-clip-margin` retirée entre-temps (inerte sur `hidden` de toute façon). Exactement le travers corrigé deux tours plus tôt sur le journal : ajouter la correction sans retirer l'affirmation périmée. Bloc réécrit d'un seul tenant · 2026-07-30 (tours 6-7) : CAUSE des verdicts dégradés trouvée — ni quota ni token expiré (les 2 pistes du rapport), mais `concurrency: cancel-in-progress: true` dans `code-review-panel.yml` : chaque push annulait le panel en cours et tuait les reviewers non terminés, d'où l'aggravation 1→2→3 manquants qui suivait ma cadence de commits (runs 14:03/14:10/14:16 tous `cancelled`). Tour 7 laissé tourner sans push : 4 reviewers au vert, verdict CONDITIONAL autoritaire (0/1/1). Ses 2 findings corrigés : (a) le tableau « historique honnête des tours » du corps de PR s'arrêtait au tour 4 alors que le journal documentait le tour 5 — même classe de péremption que celle qu'il servait à éliminer, complété jusqu'au tour 7 ; (b) `waitForFlyerWallSettled` sans try/catch dans `captureLevel` couplait une attente cosmétique (menu) à la capture du PREMIER niveau — un timeout faisait perdre `level_<premier>.png`, que rien ne retente. Isolé en try/catch, VÉRIFIÉ PAR MUTATION (sélecteur rendu introuvable + timeout 4 s : le script logue « failed 00_menu.png » et poursuit sur Stalingrad et Vitry, là où il abandonnait avant) · 2026-07-30 (tour 8, panel COMPLET : PASS — 0 bloquant / 0 majeur / 3 mineurs) : (a) tableau d'inventaire du corps de PR faux — j'avais compté 5 fichiers là où le diff en touche 8 (.ts et un 2e .css manquants) ; recalculé DEPUIS `git diff --name-only origin/main...HEAD` et non à la main, c'est le comptage manuel qui l'avait fait dériver ; (b) `overflow-x: hidden` posé sur `.rubriques` rognait les TROIS onglets alors que seul NIVEAUX dérive latéralement — correctif trop large de ma part, re-scopé sur une classe `.rubriquesLevels` appliquée uniquement quand `active === "levels"`, pour que SCORES/OPTIONS gardent une barre de défilement visible plutôt qu'un contenu coupé en silence si leur copie débordait un jour ; (c) le try/catch unique autour de `waitForFlyerWallSettled` + `screenshot()` étiquetait « flyer wall never settled » même quand c'était la capture qui échouait (disque, page fermée) — scindé en deux blocs pour que le message désigne l'étape réellement fautive · 2026-07-30 (tour 9, CONDITIONAL : 3 MAJEUR + 2 MINEUR, tous traités) : (a) MAJEUR de PROCESS — le corps de PR CONSTATAIT le critère mono-lane cassé sans jamais escalader, alors que la doctrine dit « critère cassé ⇒ pipeline complet » ; constater n'est pas appliquer. Porté à `senior-architect`, qui a rendu une DÉROGATION DE PALIER explicite et conditionnée (voir l'entrée dédiée ci-dessous) plutôt que de laisser la brèche non tranchée ; (b) MAJEUR — la checklist disait le trou de tests « laissé en l'état » alors que le diff ajoutait les 4 tests qui le comblent : ligne corrigée, seul l'auto-focus pendant l'animation reste assumé ; (c) MAJEUR — le test de stagger n'épinglait que la MONOTONIE, pas le pas : réduire le stagger à 2 ms détruisait la cascade en restant vert. `FLOAT_IN_STAGGER_MS` exporté et le pas exact asserté contre la constante (plus de nombre retapé côté test) ; VÉRIFIÉ PAR MUTATION (pas → 2 ms : rouge, alors que l'ancien test passait) ; (d) MINEUR — `.rubriquesLevels` n'avait aucune couverture : `src/render/ui/__tests__/MainMenu.test.ts` créé (3 tests), VÉRIFIÉ PAR MUTATION (typo `"level"` dans le littéral ⇒ rouge) ; (e) MINEUR — `waitForFlyerWallSettled` se résolvait À VIDE : `.every()` sur un tableau vide renvoie `true`, donc tout tick de poll sans slot déclarait « stabilisé » ; or FlyerWall est monté conditionnellement et se démonte à chaque aller-retour de rubrique. Garde `slots.length > 0` ajoutée · checks: tsc/eslint/prettier verts, `src/render/ui` 114 tests verts · 2026-07-30 (tour 10, panel COMPLET : PASS — 0 bloquant / 0 majeur / 3 mineurs, ce qui remplit la condition n°1 de la dérogation sur `caf137e4`) : (a) inventaire de la PR à nouveau faux — RÉCIDIVE de ma part : au tour 8 j'avais dit le recalculer depuis le diff, puis j'ai ajouté `MainMenu.test.ts` au tour 9 sans refaire le calcul, et un fichier CRÉÉ n'était compté nulle part (9 fichiers réels, 8 annoncés). Recalculé avec `--diff-filter=M` et `--diff-filter=A` séparément, et les deux commandes sont désormais inscrites en commentaire HTML dans le corps de la PR pour que le prochain qui touche au tableau les rejoue au lieu de compter à la main ; (b) l'index d'exports en tête d'`e2e-lib.mjs` — dont c'est le rôle déclaré d'énumérer ce que le module offre à ses 12 importateurs — omettait `waitForFlyerWallSettled` : entrée ajoutée, sinon le prochain qui cherche un helper d'attente avant d'en écrire un ne l'y trouve pas et le redéveloppe ; (c) `FLYER_FLOAT_IN_VARIANTS` typé `Record<string, string>` n'était pas vérifié sur ses 12 clés `--fio-*` : une faute de frappe passait la compilation, retombait sur le défaut CSS zéro et aplatissait silencieusement un axe de la chute. Typé par union de clés explicite (`FloatInVars`), VÉRIFIÉ PAR MUTATION (clé inconnue ⇒ `error TS2353` à la bonne ligne) · review: fix lane, la PR #145 reste en draft
- 2026-07-30 · claude/flyer-wall-float-in-animation (PR #145) · dev-tooling-assets · panel CI stage 6 PASS (0 bloquant / 0 majeur / 2 MINEUR) : les deux mineurs (`findings-code-review` + `findings-edge-case-hunter`) visaient le même défaut — le `sleep(2500)` de `scripts/screenshot-preview.mjs` rejouait TROIS valeurs possédées ailleurs (stagger 180 ms dans `FlyerWall.tsx`, durée 1400 ms dans `FlyerWall.module.css`, `LEVELS.length`) sans rien pour garder les copies synchronisées ⇒ 3 niveaux de plus et la capture en pleine animation, que ce même diff corrigeait, revenait en silence sans test pour l'attraper. Correctif VOLONTAIREMENT différent de celui suggéré par les deux reviewers (recalculer `(n-1)*stagger + durée`), qui laisse les trois constantes à synchroniser à la main dans trois fichiers — soit la même fragilité en plus petit : nouveau helper partagé `waitForFlyerWallSettled()` dans `scripts/e2e-lib.mjs` qui attend l'ÉTAT FINAL OBSERVABLE via la Web Animations API (`el.getAnimations().every(a => a.playState === "finished")` sur `.muf-flyer-slot`) ⇒ zéro arithmétique, zéro constante dérivable, et juste par construction si le stagger, la durée ou le nombre de niveaux changent ; correct aussi sous reduced-motion (aucune animation à attendre ⇒ `every` sur liste vide). Appliqué aux DEUX captures du menu, dont la `?preview=menu` autoritaire qui écrase `00_menu.png` (nouveau paramètre `settleFlyers`) · checks: eslint/prettier/vitest(94, suite scripts) verts + exécution réelle de `screenshot-preview.mjs` contre le build : `00_menu.png` stabilisée (capture inspectée) · les captures de NIVEAUX échouent en local (`locator.waitFor` 20 s / `locator.click` 30 s) À L'IDENTIQUE avec et sans ce diff — vérifié en remisant le diff (`git stash`) et en relançant : préexistant, environnement local seul, l'E2E de la CI est vert · PR: corps mis à jour avec les 2 URLs de preview (celle de la branche + celles du spike R3F `claude/spike-r3f-flyers`, vérifiées HTTP 200)
- 2026-07-30 · claude/flyer-wall-float-in-animation (PR #145) · dev-r3f-render + dev-tooling-assets · panel CI stage 6 FAIL (1 BLOQUANT / 2 MAJEUR / 4 MINEUR) → traité : (BLOQUANT, `findings-bmad-review`) l'entrée de journal du 2026-07-26 racontait « sleep 2500ms ajouté » alors que la PR livre en réalité `waitForFlyerWallSettled()` — le journal, dont c'est justement le rôle d'audit, donnait une version FAUSSE du mécanisme, et qui plus est celle que le correctif final élimine ; ligne réécrite pour dire que le `sleep` du commit `c0d0593a` a été REMPLACÉ dans la même PR, avec renvoi vers l'entrée du 2026-07-30 · (MAJEUR, `findings-bmad-review`) la case « un seul lane dev propriétaire » était devenue FAUSSE : le correctif du panel a lui-même déplacé le fix dans `scripts/` (helper exporté + 2 sites d'appel du harness E2E partagé), donc le diff touche `src/render/ui/menu/**` (dev-r3f-render) ET `scripts/**` (dev-tooling-assets) → case DÉCOCHÉE dans le corps de la PR, encadré explicite « le critère mono-lane est cassé, ce n'est plus une course express », validation `dev-tooling-assets` sur la partie harness laissée à Bertrand · (MAJEUR, `findings-edge-case-hunter`) débordement horizontal transitoire de `.rubriques` pendant l'entrée (34→50px mesurés au tour précédent et laissés hors périmètre) : `.rubriques` ne déclarait qu'`overflow-y: auto`, donc `overflow-x` calculait à `auto` et la dérive latérale des flyers (±44px, variante B) pouvait faire clignoter une barre horizontale ~2 s — corrigé par `overflow-x: hidden` sur `.rubriques`. Écrit `hidden` et NON `clip` volontairement : avec une valeur scrollante sur l'autre axe, la spec fait calculer `clip` en `hidden` de toute façon (vérifié dans le navigateur : `getComputedStyle().overflowX` renvoyait bien `hidden` alors que la feuille disait `clip`), donc écrire `clip` aurait décrit un comportement que la page n'obtient jamais ; le rack short-landscape n'est pas affecté, `.muf-flyerwall` est un descendant avec son propre `overflow-x: auto` · (MINEUR) les 12 défauts `--fio-*` de `.slot` recopiaient à l'identique `FLYER_FLOAT_IN_VARIANTS[0]` : ils ne peuvent pas être supprimés (l'index est `| undefined` sous `noUncheckedIndexedAccess`, et un `var()` sans valeur ni fallback rend tout le `transform` invalide) mais ils ne doivent pas non plus redire de vraies valeurs — passés à ZÉRO neutre : plus rien à faire dériver, et le cas dégénéré se dégrade en « apparaît en place » au lieu d'un transform cassé · (MINEUR) aucun test ne couvrait le stagger ni le garde-fou reduced-motion → 4 tests ajoutés dans `FlyerWall.test.ts` (présence de `--slot-delay` sur chaque slot, délais strictement croissants en partant de 0, trajectoires différentes entre flyers consécutifs, classe `.slot` toujours émise pour que les deux kill-switches restent atteignables) · checks: tsc/eslint(0 erreur)/prettier/vitest(1266) verts + mesure NAVIGATEUR du correctif de débordement sur 390×844, 1280×800 et le rack 844×390 : aucune barre horizontale peinte (`offsetHeight - clientHeight === 0`) sur les trois · tests validés par MUTATION : stagger mis à 0 ⇒ 1 rouge, cycle des variantes remplacé par `[0]` ⇒ 1 rouge, code restauré ⇒ 16 verts · NON corrigés, assumés et argumentés : (a) l'auto-focus du premier lancement part pendant l'animation — le correctif proposé (différer jusqu'à la fin de l'entrée) ferait arriver le focus ~2 s après le montage, ce qui est un arbitrage d'accessibilité pour `ux-designer`, pas une évidence technique : un focus tardif est plausiblement PIRE pour un lecteur d'écran que la cible de scroll transitoire qu'il corrige ; (b) l'entrée rejoue à chaque retour sur l'onglet NIVEAUX — comportement voulu (Bertrand l'a vu rejouer à chaque rechargement pendant les 3 tours d'itération sans le signaler), mais c'est un appel de design non documenté jusqu'ici, donc noté ici plutôt que gaté unilatéralement
- 2026-07-30 · claude/flyer-wall-float-in-animation (PR #145) · senior-architect · DÉROGATION DE PALIER accordée sur le critère mono-lane cassé : le diff touche `src/render/**` (dev-r3f-render) ET `scripts/**` (dev-tooling-assets), et la course express reste néanmoins applicable — par décision explicite, pas par omission. MOTIF : (a) la moitié `scripts/` est PUREMENT ADDITIVE sur le harness partagé (un export ajouté à `e2e-lib.mjs`, aucun export existant ni état de module modifié) ⇒ les 12 autres importateurs, dont les gates bloquants `e2e-home`/`e2e-ingame`/`e2e-delivery`/`harness-golden`/`harness-assert`/`align-*`, sont inchangés ; (b) l'unique consommateur est `screenshot-preview.mjs`, lui-même appelé par le seul `preview.yml`, qui publie un artefact téléchargeable et ne commite rien — pire cas réel : `00_menu.png` absente d'un artefact, loguée, les captures de niveau préservées par l'isolation en deux try/catch ; (c) le helper n'a aucune existence autonome — il attend une condition créée par la moitié render, via `.muf-flyer-slot`, classe globale possédée par le render et déjà épinglée par `FlyerWall.test.ts`, même idiome que `TITLE_MARKER`/`MENU_MASTHEAD` déjà présents dans `e2e-lib.mjs` ⇒ AUCUNE nouvelle couture d'architecture. Le critère mono-lane est une heuristique de CHEMIN qui approxime le rayon de souffle : ici l'heuristique se déclenche, le rayon de souffle non. Le seul déficit réel du palier express (une paire d'yeux sur du code harness) est sur-couvert : panel CI 4 reviewers, 8 tours, dernier COMPLET et PASS. CONDITIONS DE VALIDITÉ — la dérogation est NULLE si (1) le panel COMPLET n'est pas re-gagné sur le diff FINAL ; (2) un export EXISTANT de `e2e-lib.mjs` est modifié, un état de module y est ajouté, ou un script consommé par un gate bloquant est touché ; (3) le diff gagne un 3e lane (`src/game/**`, `.github/workflows/**`, `levelArt.json`). Signature `dev-tooling-assets` NON requise, remplacée par cette dérogation. HORS PÉRIMÈTRE, ne bloque pas le merge : ticket `ux-designer` à ouvrir sur l'auto-focus du premier lancement qui part pendant l'entrée et sur le rejeu de l'entrée à chaque retour sur NIVEAUX. ÉCART DE DOCTRINE relevé pour `producer` : le palier fix-lane n'offre que deux issues (express / pipeline complet) ; il manquait ici la troisième — « express multi-lane à double signature » — qui aurait évité la dérogation. À porter dans COLLABORATION.md §fix lane · ruling: WAIVER — senior-architect (Winston)

## Tour 12 (panel COMPLET, PASS — 0 bloquant / 0 majeur / 2 mineurs)

L'escalade est acceptée par le panel. Les 2 mineurs traités :

- **Inventaire de la PR faux pour la 3e fois** (tours 8, 10, 12). Cause à chaque fois la
  même : j'ai PRÉDIT les chiffres au lieu de lancer la commande que j'avais moi-même
  inscrite dans le corps. Cette fois le piège était subtil — `fixes.md`, vidé de mes
  ajouts lors de l'escalade, redevient identique à `main` et **sort du diff**, alors que
  je le comptais encore comme modifié. Le tableau est désormais **généré** par script
  depuis `--diff-filter=M` / `=A`, plus rédigé à la main.
- **Plafond du timeout de `waitForFlyerWallSettled`.** Le finding est juste : les 20 s ne
  dérivent ni du stagger ni du nombre de niveaux, et cesseraient de couvrir le mur vers
  ~105 niveaux. Choix assumé de **documenter le plafond plutôt que de le dériver** :
  calculer `(n-1) x stagger + durée` réintroduirait dans `e2e-lib.mjs` exactement le
  couplage à trois sources que ce helper existe pour supprimer (stagger dans le TSX,
  durée dans le CSS, compte dans les données). L'attente elle-même passe déjà à l'échelle
  gratuitement puisqu'elle observe l'état final réel ; seul le garde-fou est fixe, et le
  commentaire dit quoi faire si le jeu s'en approche.

## Tour 13 (CONDITIONAL — 1 majeur, 2 mineurs, tous traités)

- **MAJEUR — suppression de capture trop zélée, régression que J'AI introduite** en
  corrigeant le majeur du tour 11. Mes deux correctifs combinés rendent le `rm`
  contre-productif : depuis que `captureLevel` attend lui aussi le settle ET saute sa
  capture quand l'attente échoue, un `00_menu.png` en pleine animation **ne peut plus
  exister**. Supprimer le fichier lors de la seconde passe ne détruisait donc plus une
  capture périmée mais une capture BONNE, sur un échec sans rapport (runner lent, hoquet
  sur `?preview=menu`). `rm` retiré, l'erreur remonte simplement au catch externe.
- **MINEUR — le plafond documenté était faux** : les 20 s étaient dépensées DEUX fois
  (montage puis settle), soit ~40 s réelles contre les 20 s annoncées, et mon calcul des
  ~105 niveaux reposait sur un seul budget. Les deux attentes partagent désormais une
  échéance unique, donc le chiffre documenté est le vrai.
- **MINEUR — les 12 défauts `--fio-*` neutres** doublaient ce qu'un repli `var(x, 0px)`
  donne gratuitement. Remplacés par des replis à l'usage dans le keyframe ; il n'y a plus
  de seconde liste à tenir à jour. **Vérifié en navigateur** (et pas seulement en test
  unitaire, qui n'aurait pas vu un `var()` cassé) : 5 slots déplacés en vol avec 5
  transforms DISTINCTS — les variantes s'appliquent toujours — et tous à l'identité une
  fois stabilisés.
