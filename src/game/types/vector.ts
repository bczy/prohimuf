/**
 * A 2-dimensional vector used for all positional data in game logic.
 * Lives in src/game/ — zero React/Three.js imports ever.
 */
export interface Vec2 {
  readonly x: number;
  readonly y: number;
}
