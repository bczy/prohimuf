# NNNN — Title

- **Status:** Proposed | Accepted | Superseded by ADR-XXXX
- **Date:** YYYY-MM-DD
- **Number:** NNNN, allocated by `producer` at story opening (recorded in
  `docs/handoffs/story-<slug>.md`), re-checked at merge. (If self-allocated because no
  producer was in the loop, say so here.)

## Context

What forces are at play — the problem, the constraints, the prior state. Enough that a
future reader understands why this decision was necessary without re-deriving it.

## Decision

What we decided to do. Concrete and unambiguous — the deciding lane owns this content.

## Consequences

What follows — positive, negative, and any gotchas to watch for (new surfaces to keep
alive, seams to serialise, follow-up work, things that will now fail loudly).
